[package]
name = "Proxy"
version = "0.1.0"
edition = "2024"

[dependencies]
native-tls = "0.2.18"
tokio = { version = "1.52.3", features = ["full"] }
tokio-native-tls = "0.3.1"

[build-dependencies]
winres = "0.1.12"
